# Samples

Files in this folder are bundled with Moments and copied into the app's writable Samples library on startup.

Add JPG, PNG, WEBP, BMP, GIF, TIFF, or supported video files here if you want them included with the app.

At runtime, Moments copies missing files from this repo folder into the writable app-data Samples folder so users can also drop in extra files locally.
